# 26.1 Fabric migration — work in progress

This branch starts at `1.21.1-fabric` commit `d09d86476ab15d8c9d8ccd9014abd492dc693f8e`.
The production core baseline is 2.4.1, `9a37b6983bf032342a05b445a84d8f009cbb5f86`.
The isolated workspace is `F:/codex-cache/halo-26.1-migration`.

The local core build-only commit `295bc579ab8d63e6dba5642ebb9e889516e90105` adds the explicit JUnit launcher required by Gradle 9.
No production source, public descriptor or Java 17 contract changed. It is not published;
artifacts referencing this commit are development packages, not releases.

Target: one Java 25 JAR for Minecraft 26.1, 26.1.1 and 26.1.2, adapter revision 1.
Toolchain: Gradle 9.5.1, Loom 1.17.19 (unmapped), Loader 0.19.3,
Fabric API 0.155.3+26.1.2. YSM is explicitly excluded. Connector is outside this acceptance scope.

## Behavior and evidence ledger

Passing a build is not a game acceptance result. All rows remain open until their applicable
runtime cases have evidence. Worlds and configurations are isolated by game version.

| Original behavior | Target implementation | Automatic coverage | Runtime evidence / status |
| --- | --- | --- | --- |
| Show/hide, source priority, persistence | Existing core runtime, new network and SavedData adapters | Core ownership/lifecycle, packet bytes, NBT format | 26.1.2 show and restart restore observed; remainder pending |
| Death/respawn, dimension change, unload/rejoin | Server events and teleport hooks | Core lifecycle | Pending |
| Namespace search, completion, @s, local mode | Existing command rules with Mojang platform names | Command/parser tests | 26.1.2 @s show observed; other cases pending |
| Scepter, recipe, components, GUI | Native 26.1 item and GUI extraction | Approved recipe layout | Recipe load fixed; interactions pending |
| Resource priority/reload/recovery | Definition snapshots and native resource reload | Existing resource/core tests | Initial 12 definitions loaded; reload pending |
| Animation, physics, sleep, invisibility | Core 2.4.1, one world frame submission | Core motion/render tests | Pending |
| Billboard/ring/OBJ, mask and UV animation | Native RenderPipeline/RenderPass | Material/shader, actual-depth sorting | Default ring shown; remaining geometry pending |
| Transparency and cached backend | Static VBO/EBO, revision-tracked dynamic EBO, CPU fallback | Sort/cache recovery tests | Pending |
| First person camera | Camera position and quaternion-to-head basis | Vertical pitch/yaw wrap/roll basis regression | Pending |
| Inventory previews | Isolated core sessions and final PIP model capture | Preview math/session tests | Creative preview observed; survival and custom EMF model pending |
| API v2 anchors and ownership | Existing core API, platform extraction/draw scopes | Frozen caller/core boundary checks | External provider runtime pending |
| Iris/LabPBR, IterationRP | Entity pipeline assignment and private shader variants | Generic derivative normal matcher and material sampling | Pending |
| EMF/ETF | Mojang ABI gate, captured world and preview head | Capture math/ABI gate | Pending |

## Verification runs

- Standalone core: Java 17 / core wrapper build passed; log `F:/codex-cache/halo-261-core17.log`.
- Main source compilation: passed on 26.1.2.
- Combined build, tests, distribution and frozen API callers: passed on 26.1.2; current log `F:/codex-cache/halo-261-build.log`.
- Native 26.1.2: startup, resource load, integrated world join, show and restart restoration observed.
  Early runtime fixes: recipe ingredient format, lazy texture upload outside an active RenderPass.
  Logs: `F:/codex-cache/halo-261-client*.log`; these are iterative development runs, not final acceptance.
- Native 26.1 and 26.1.1: not run yet.
- Dedicated dual clients and no-Halo-server local mode: not run yet.
- Iris 1.11.4, Sodium 0.9.2, EMF 3.3.8, ETF 7.2.4 used in the 26.1.2 compatibility run;
  exact metadata at `F:/codex-cache/halo-26.1-compat/*-version.json`.
- LabPBR channel isolation, POM and optimization measurements: not run yet.
- Sodium 0.8.9 was rejected by Iris 1.11.4 dependency validation. The tested 0.9.2 combination
  only establishes 26.1.2 compatibility; earlier patch versions still need matching dependencies.

No push, tag or release is authorized for this task. Migration is not complete.

## 2026-09-19 rendering regression fixes

The user reported: transparent mesh faces were obscured by clouds without shaders and almost
invisible against the sky with IterationRP; the scepter item model was missing; smooth normals
were lost under IterationRP. After restarting with these fixes, the user confirmed all three
reported problems had disappeared. This confirmation applies to this scene and combination,
not to the complete migration acceptance matrix.

- Deferred meshes now draw after `FrameGraphBuilder.execute`, after clouds/weather and vanilla
  transparency resolve, before Iris composites and the world camera transform is removed.
  Core is not advanced again in this hook; per-instance and per-triangle view-depth sorting remains.
- The scepter has the native `assets/halo/items/halo_scepter.json` model entry, referencing the
  existing item model and texture. Held appearance was also observed by the agent.
- Halo wraps native/Iris pipeline selection so its cloned programs cannot be bypassed by another
  HEAD injection's early return. Lit clones use entity solid/translucent programs. Fullbright
  clones use the original adapter's particles semantics, linked against Halo's entity vertex layout.
- The generic derivative normal pattern matched both lit variants of IterationRP Alpha 0.8.28.
  Logs confirm private-program selection during drawing, not only shader compilation.
- HaloMask reserves texture unit 3 only inside private shader construction. Native samplers remain
  on 0/1/2 even when unused uniforms are optimized out; Iris allocates PBR samplers around the
  reserved mask unit. Other Iris programs remain unchanged.
- The normal patch requires a matching vertex ABI. Unrecognized normal reconstruction remains
  unchanged; the code has no shader-pack name check. Temporary absolute-path shader dumps removed.

Evidence: `F:/codex-cache/halo-261-render-fixes-client.log`,
`F:/codex-cache/halo-261-render-fixes-build.log`, and isolated client screenshots
`2026-09-19_17.17.00.png` (IterationRP, scepter) / `2026-09-19_17.19.01.png` (no shaders).
Exact runtime: Minecraft 26.1.2, Loader 0.19.3, Fabric API 0.155.3+26.1.2,
Iris 1.11.4+mc26.1.2, Sodium 0.9.2+mc26.1.2, EMF 3.3.8, ETF 7.2.4.
The latest shader whitespace/vertex-ABI guard is additionally covered by platform tests.

## Known limits before acceptance

- Resident world geometry falls back to CPU expansion with Iris enabled so Iris can generate
  per-face tangent/mid-UV attributes. Native resident mesh and cached primitives remain available.
  Performance parity and allocation/upload measurements are pending.
- Network, dedicated dual-client, no-Halo-server local mode and the full lifecycle matrix have
  not been executed. Minecraft 26.1 and 26.1.1 game runs have not been executed.
- Complete LabPBR channel, custom EMF model, camera roll/extreme-pitch and resource-recovery
  matrices remain pending; observing one working scene does not close them.
- `development_build=true` keeps locally committed packages explicitly marked `.dev` until
  runtime acceptance and publication of the build-only core commit. The release gate remains active.

Latest automatic result: 117 platform tests and 326 core tests, zero failures/errors/skips.
Frozen anchor API consumer, core frozen callers and flat-JAR provenance checks passed.
