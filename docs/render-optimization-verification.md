# 2.3.1 渲染优化与验收记录

本轮只跟进 Minecraft 1.20.1 Fabric 主线及其锁定的 core，功能版本 `2.3.1`、适配版本 `2.3.1+adapter.1`。基线是 Halo `f4523c443087e4eb05c51476d6e5771653ca8d3c` / core `4b4f7bb1f7dea99d86fd46d451f5bc4b016404eb`。不修改 flash 分支、API v2、定义 JSON、存档或网络协议。

## 使用方式

`config/halo-azusake/halo_mod_config.json` 中的 `primitiveRenderBackend` 支持：

| 值 | 行为 |
| --- | --- |
| `compatibility`（默认） | 共用预生成的局部几何，展开为原有即时顶点；只合并相邻且绘制状态相同的批次 |
| `cached` | 使用静态 GPU 几何和逐图元变换；沿用旧图元材质、原顺序和提交阶段 |

`/halo renderer` 查询；`/halo renderer compatibility` 或 `/halo renderer cached` 切换并保存。缺失、null、非字符串或未知值均恢复为 `compatibility`，其他配置和未知字段保留。命令不要求服务器权限，也不转发到服务器。下一帧开始时一次采样设置，同时应用于世界和 GUI；不会重置佩戴、物理或动画。已有 OBJ mesh 不受该设置影响。

兼容模式适合大量同材质、相邻 billboard；缓存模式在较多 ring 顶点或材质交替时可能更合适。缓存模式不是统一更快的开关。若某个光影组合出现显示问题，可立即切回兼容模式；此开关不替代回归修复或验收。

## 实现边界

- core 按形状参数共享不可变局部几何。ring 内外面保留独立法线、原绕序、分段数和接缝 UV；billboard 兼容输出仍是四边形，缓存三角形对角线为 `0,1,2 / 2,3,0`。`face_camera` 继续使用世界/正交预览的原有朝向计算。
- 兼容展开只计算一次共享顶点，保留完整源顶点序列。纹理可用性只在单次渲染调用内去重。相邻合并核对纹理、拓扑、材质、光照、方向光、统一颜色/透明度和深度/剔除状态；顶点自己的颜色仍逐顶点保留。
- 缓存提交复用 `HaloMeshBufferCache`、原生/Iris 材质及状态恢复设施。保持旧透明度截断、顶点颜色的字节量化和光照顺序。旧图元不进入 OBJ 透明排序队列，不逐帧排序或上传静态索引，也不对镜像变换额外翻转绕序。
- 受光 billboard 上传时保留四角输入，使 Iris 继续生成原来的切线/UV 中点，再使用同一对角线索引。共用上传暂存 builder 在成功/异常路径显式释放 `GlAllocationUtils` 的原生 malloc；GPU 同步上传完成后才释放，不依赖无 cleaner 的 ByteBuffer 被 GC。
- 在客户端资源加载/定义刷新时准备几何、纹理，缓存模式的 GPU 上传发生在渲染线程。切回兼容模式释放图元 GPU 缓冲；资源代次、定义移除、光影格式变化及客户端退出负责更新/释放。专用服不准备绘图几何。
- 上传异常被记录并记忆，重载或重新启用缓存模式时重试；缺失或代次不符的缓冲在提交前从同一帧命令展开回退。回退不再调用模拟、不重复绘制。未纹理化或缺少相应原生光照格式的命令继续使用兼容提交。
- 旧 `FrameScene`、`PreviewFrame`、`FrameOutput` 构造器和访问器保留。新模式只输出一种旧图元表示；`expandedBatches()` 同时支持新命令。`DefinitionSnapshot` 原有 mesh 依赖含义不变，另提供只读旧图元依赖和几何入口。

## 自动验证

Java 17 独立 core 构建、Halo 联合构建与打包检查；314 项 core 测试及 108 项 Halo 测试均通过，无失败/错误/跳过。Halo 测试提供了实际 YSM 2.6.5 发布 JAR，签名检查未跳过。另通过平台边界扫描、最终 JAR 合包/来源检查、旧锚点 API v2、外部预览提供者、旧 ClientPort 以及本轮新增的冻结 2.3.0 帧/预览/输出构造器二进制调用方检查。

冻结旧 `SceneRenderer` 和旧 collector 为独立数值参照。固定时间/实体/相机，对照原实现、兼容模式和缓存展开，检查位置、UV、颜色、状态、顺序、有效法线及物理输出；位置/法线浮点容差为 `2e-6`。覆盖发光/受光、缺失纹理、双纹理与双面 ring、UV 接缝、面对相机、非均匀/镜像/零缩放、大坐标和透明度。另检查配置修复/保存、共享资源、矩阵防御性复制、表示互斥、预览切换不重置状态、合批边界、上传失败记忆与资源释放。

## 实际环境

隔离原生客户端及用户指定 `1.20.1-Fabric 0.19.3` 实例的副本；原实例未替换模组或改写存档。测试使用 Java 17.0.11、Windows、i9-14900HX、RTX 4060 Laptop GPU（驱动 32.0.16.1664），1280×720，视距/模拟距离 12，VSync 关闭，`maxFps=260`（原版不限帧档）。整合包包括 Sodium 0.5.13、Iris 1.7.6、EMF 3.1.1、ETF 7.1、YSM 2.6.5，以及原配置的 iterationRP Alpha 0.8.28。

测试探针、定义及截图只存在于隔离验证目录，不进入 Halo 成品。世界测试混用面对相机的 billboard、发光及双纹理受光 ring、OBJ mesh，并使用 255/128/12 alpha 的纹理核对透明边缘。所有截图均同步检查 GL 错误。

实际游戏验证使用同一套生产字节码，2026-09-16 最终回归全部正常退出。自动游戏探针只负责复现操作、断言和保存截图，截图另行人工式目视核对；动画截图未锁定到同一时刻，因此不以逐像素相等作为判定。

| 环境 | 已执行内容 | 结果 |
| --- | --- | --- |
| 隔离原生客户端 | 两种模式、第一/第三人称、混合图元、受光/发光、透明边缘、预览及资源重载 | 未观察到新增显示问题，截图 GL error 均为 0 |
| EMF 3.1.1 + ETF 7.1 专项 | 带头部偏移/动画的自定义玩家模型，两种模式的世界、预览及重载 | 世界锚点实际捕获到 EMF；预览模型与光环正常显示 |
| 指定整合包副本 | Sodium/Iris/EMF/YSM 共存，iterationRP 开启；两种模式的世界与预览、运行中关闭光影、重载 | 实际捕获到 YSM 世界锚点，YSM 物品栏模型正常；未见图元消失或新增半透明异常 |
| Fabric 专用服 + 两客户端 | 两名无 OP 玩家分别在两种模式下切换、预览、重载、显隐、隐身、睡眠/醒来、死亡重生、换维度、重连 | 配置保存、佩戴恢复和存活断言通过；渲染命令未发送到服务器 |
| 无 Halo 的原版专用服 + 本地模式客户端 | 同上，两种模式均完整执行 | 本地佩戴及客户端指令正常，不需要服务端 Halo 或权限 |
| 缓存故障与资源生命周期 | 清空/代次不符缓存；在 flat 上传后、lit 上传前注入异常；切回兼容再启用、资源重载及光影格式重建 | 同帧展开回退可见，重试恢复；兼容模式旧图元 GPU 顶点数为 0，每次截图时上传暂存 builder 均已释放 |

专用服的首轮换维度脚本让生存玩家从高处跌落，未将该死亡画面计为通过；修正为安全平台和存活断言后，两名客户端及本地模式在两种后端下均重新完成。离线测试账号的认证 401、第三方缺失可选兼容类/数据修复器及已有资源错误保留在日志中，不记作 Halo 回归。注入上传失败的预期错误亦保留。

本次覆盖上述具体版本和场景，不代表所有光影、模型、材质包或多人整合组合；没有进行长时间稳定性浸泡。遮挡/透明状态的数值与提交契约有自动回归保护，尚无覆盖所有遮挡角度的逐像素游戏图像测试。用户原整合包中偶发的转回视角卡顿仍未确定根因，不能声称已全部消除。未发现已确认且未修复的本轮回归。

## 性能方法

每组固定世界、机位、资源、画质和探针。六个场景分别为：20 个光环 × 32 billboard、20 × 8 同纹理 ring、20 × 8 交替纹理 ring、1 × 128 ring、1 × 32 billboard、1 × 8 ring。后两组用于光环数量增长对照，最后一组与 128 ring 场景用于单光环图元数量对照。

各场景预热 3 秒，再测 3 轮，每轮 5 秒；第 2–3 秒看向无测试光环区域，然后转回。帧时间来自相邻 `GameRenderer.render` 完成时刻，CPU/线程分配来自 `HaloRenderer.renderHalos` 的探针，包含旧图元提交和物理，但不包含后续独立的 OBJ 延迟提交。上传量统计该阶段的顶点缓冲上传字节，提交数避免重复计入即时提交内部的 VBO draw。缓存静态上传另由资源日志核对。

所有版本都开启相同 JFR 设置。每轮记录中位数、P95、P99、最大值；汇总采用三轮统计值的中位数，峰值取三轮最大值。不同帧率下不能直接比较整段 JFR 的累计分配量，应使用每帧分配量。测试为合成场景的本机结果，不能替代所有整合包/资源包的测量。

## 三组性能结果

完整逐轮数据见 [CSV](render-optimization-performance.csv)，包含全部六个场景的 P50/P95/P99/峰值、Halo CPU、分配、提交、上传及转回后 200 ms 的峰值。下表采用三轮中位数，分配量为每帧 KiB。


### 原生

| 场景 | 模式 | 帧中位 ms | Halo CPU ms | 分配 KiB/帧 | 提交/帧 | 上传 KiB/帧 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| 20 × 32 billboard | baseline | 4.045 | 3.264 | 1437.9 | 640 | 60.0 |
| 20 × 32 billboard | compatibility | 0.874 | 0.426 | 1272.8 | 1 | 60.0 |
| 20 × 32 billboard | cached | 3.043 | 2.401 | 1652.4 | 640 | 0.0 |
| 20 × 8 同纹理 ring | baseline | 5.225 | 4.447 | 4833.2 | 320 | 1440.0 |
| 20 × 8 同纹理 ring | compatibility | 3.077 | 2.407 | 2507.7 | 1 | 1440.0 |
| 20 × 8 同纹理 ring | cached | 1.642 | 1.152 | 674.7 | 320 | 0.0 |
| 20 × 8 交替纹理 ring | baseline | 5.097 | 4.309 | 4833.2 | 320 | 1440.0 |
| 20 × 8 交替纹理 ring | compatibility | 3.679 | 3.030 | 2640.7 | 160 | 1440.0 |
| 20 × 8 交替纹理 ring | cached | 1.660 | 1.153 | 674.8 | 320 | 0.0 |
| 1 × 128 ring | baseline | 4.210 | 3.485 | 3841.5 | 256 | 1152.0 |
| 1 × 128 ring | compatibility | 2.437 | 1.857 | 1981.6 | 1 | 1152.0 |
| 1 × 128 ring | cached | 1.357 | 0.897 | 515.1 | 256 | 0.0 |

### 整合包 + iterationRP

| 场景 | 模式 | 帧中位 ms | Halo CPU ms | 分配 KiB/帧 | 提交/帧 | 上传 KiB/帧 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| 20 × 32 billboard | baseline | 13.200 | 8.008 | 2091.6 | 640 | 60.0 |
| 20 × 32 billboard | compatibility | 4.121 | 0.576 | 1283.9 | 1 | 60.0 |
| 20 × 32 billboard | cached | 9.932 | 5.824 | 2807.1 | 640 | 0.0 |
| 20 × 8 同纹理 ring | baseline | 11.835 | 7.415 | 5130.4 | 320 | 1440.0 |
| 20 × 8 同纹理 ring | compatibility | 6.066 | 2.768 | 2517.4 | 1 | 1440.0 |
| 20 × 8 同纹理 ring | cached | 6.363 | 2.914 | 1218.2 | 320 | 0.0 |
| 20 × 8 交替纹理 ring | baseline | 11.482 | 7.370 | 5130.5 | 320 | 1440.0 |
| 20 × 8 交替纹理 ring | compatibility | 8.386 | 4.529 | 2945.9 | 160 | 1440.0 |
| 20 × 8 交替纹理 ring | cached | 6.295 | 2.902 | 1216.0 | 320 | 0.0 |
| 1 × 128 ring | baseline | 9.957 | 5.983 | 4077.8 | 256 | 1152.0 |
| 1 × 128 ring | compatibility | 5.757 | 2.424 | 1988.1 | 1 | 1152.0 |
| 1 × 128 ring | cached | 5.557 | 2.321 | 947.4 | 256 | 0.0 |

默认兼容模式的收益可以重复观察到，主要来自连续提交合并和共享顶点计算。多纹理交替时保留顺序，所以不会合并成一个批次。缓存模式不重复上传静态旧图元；ring 的计算/分配成本下降，但它仍逐图元提交，大量相邻同材质 billboard 明显更适合兼容模式。

整合包小负载场景的整帧中位数约 4 ms，差异受其他模组/GPU 和调度影响，不能将 Halo CPU 降低直接换算为整帧收益，也不宣称所有场景 FPS 都提高。第一轮与最终每帧逻辑复测均保留；首轮缓存的密集 ring 整帧结果波动较大，不据此宣称稳定优于兼容模式。

性能测量构建 SHA-256：`b1ffd2c7f977add680407b3d0e32676c09f986383042929e6cc1420860f90a61`；基线 SHA-256：`182945fa254cdf8b4e40f38ed2497b1cfbbdf3a5d800b072890b7d2a61aa71e7`。测量后仅补充资源上传阶段的四角属性保留和原生暂存释放；上述样本不用于声称这两项修正的加载耗时收益，最终上传/释放路径另以游戏重载与故障注入验证。

### 首次加载与转回视角

基线/兼容/缓存均分别记录从启动到退出的 JFR，并重复预热后的“看向空区 1 秒 → 转回”。采样窗口内没有发现 Halo 渲染栈中的 PNG/OBJ 文件读取；两处运行各有 2 次 Guava 类加载读取，总耗时各约 0.02 ms。其余 Halo 文件读取集中在资源/类准备阶段。这不代表所有磁盘 I/O 都能被 JFR FileRead 捕获，也不能证明用户原整合包的卡顿根因就是纹理加载。

用逐轮 CSV 的写入时间近似还原采样区间后，原生基线/兼容/缓存的最大 GC 停顿约为 9.55/7.83/8.84 ms，整合包为 34.46/14.19/13.93 ms。使用 `GCPhasePause` 区分真正停顿与包含并发阶段的 GC 总持续时间。转回后的单次峰值仍有波动，并非每次都随中位数下降；详细数值保留在 CSV，不宣称卡顿完全消除。

## 交付状态

core 提交为 `7cb28c1d15f730e22a599fde0ea930cd87bd9452`。按 core 先、Halo 精确 gitlink 后的顺序进行本地提交并重新构建来源信息；不推送，不创建标签或 Release，不移动既有标签。成品哈希、最终 Halo SHA、游戏测试构建及成品字节码核对随构建清单交付。

验收材料包括本记录、108 行逐轮性能 CSV、原始逐帧 CSV、JFR 摘要、游戏截图/日志和隔离探针源码。原始 JFR 单独保存在本机验证目录，避免把整套第三方模组、用户实例配置或存档打入验收包。
